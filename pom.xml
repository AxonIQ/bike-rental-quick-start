<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!-- tag::spring-boot-starter-parent[] -->
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<!-- end::spring-boot-starter-parent[] -->

	<groupId>io.axoniq.demo.bikerental</groupId>
	<artifactId>bike-rental</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>bike-rental</name>
	<description>Demo project for Axon</description>
	<packaging>pom</packaging>

	<!-- tag::modules[] -->
	<modules>
		<!-- tag::mod-payment[] -->
		<module>payment</module>
		<!-- end::mod-payment[] -->
		<!-- tag::mod-rental[] -->
		<module>rental</module>
		<!-- end::mod-rental[] -->
		<!-- tag::mod-core-api[] -->
		<module>core-api</module>
		<!-- end::mod-core-api[] -->
		<!-- The microservices module demonstrates evolving the monolith into separate
		     deployables and is still built on Axon Framework. It is excluded from this
		     raw-gRPC rewrite. -->
	</modules>
	<!-- end::modules[] -->

	<!-- tag::properties[] -->
    <properties>
		<!-- tag::props-java21[] -->
		<maven.compiler.source>21</maven.compiler.source>
		<maven.compiler.target>21</maven.compiler.target>
		<java.version>21</java.version>
		<!-- end::props-java21[] -->
		<!-- tag::props-axonserver-connector[] -->
		<!-- The Axon Server Java connector exposes the raw Axon Server gRPC API
		     (command/query/event channels) without the Axon Framework on top. -->
		<axonserver-connector.version>2025.2.1</axonserver-connector.version>
		<!-- end::props-axonserver-connector[] -->
    </properties>
	<!-- end::properties[] -->

    <!-- tag::dependencyManagement[] -->
    <dependencyManagement>
        <dependencies>
            <!-- tag::deps-axonserver-connector[] -->
            <dependency>
                <groupId>io.axoniq</groupId>
                <artifactId>axonserver-connector-java</artifactId>
                <version>${axonserver-connector.version}</version>
            </dependency>
            <!-- end::deps-axonserver-connector[] -->
        </dependencies>
    </dependencyManagement>
    <!-- end::dependencyManagement[] -->

    <!-- tag::dependencies[] -->
	<dependencies>
		<!-- tag::deps-springboot-docker-compose[] -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-docker-compose</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<!-- end::deps-springboot-docker-compose[] -->
	</dependencies>
	<!-- end::dependencies[] -->
</project>
